# ⚡ Быстрая настройка Railway

## 🚀 Шаг 1: Получите Railway URL

1. Откройте [Railway Dashboard](https://railway.app/dashboard)
2. Выберите ваш проект
3. Нажмите на сервис с `server.js`
4. Перейдите в **"Settings"** → **"Networking"**
5. Найдите **"Public Domain"** и скопируйте URL
   - Пример: `https://dostavka-production.up.railway.app`

## 📝 Шаг 2: Установите Railway URL в коде

1. Откройте файл `js/config.js` в вашем проекте
2. Найдите строку:
   ```javascript
   window.RAILWAY_API_URL = 'YOUR_RAILWAY_URL';
   ```
3. Замените `YOUR_RAILWAY_URL` на ваш Railway URL:
   ```javascript
   window.RAILWAY_API_URL = 'https://dostavka-production.up.railway.app';
   ```
4. Сохраните файл

**⚠️ Важно:** URL должен быть **БЕЗ** `/api` в конце!

## 🔧 Шаг 3: Настройте переменные на Railway

В Railway Dashboard добавьте переменные:

| Переменная | Значение |
|------------|----------|
| `JWT_SECRET` | Случайная строка (например: `a1b2c3d4e5f6...`) |
| `NODE_ENV` | `production` |
| `DATABASE_URL` | Автоматически создается Railway |

**Как создать JWT_SECRET:**
- Используйте: https://www.uuidgenerator.net/
- Или команду: `openssl rand -base64 32`

## ✅ Шаг 4: Закоммитьте и задеплойте

1. Закоммитьте изменения:
   ```bash
   git add js/config.js
   git commit -m "Настроен Railway URL"
   git push
   ```

2. Vercel автоматически задеплоит новую версию

3. Railway автоматически перезапустит сервер

## 🧪 Шаг 5: Проверьте

1. Откройте ваш сайт на Vercel
2. Откройте консоль браузера (F12)
3. Проверьте, что видно:
   - `✅ Railway API URL установлен: https://ваш-url.railway.app`
   - `API_MODE: api`
   - `API_BASE_URL: https://ваш-url.railway.app/api`

4. Попробуйте зарегистрироваться
5. Проверьте логи Railway - должен быть запрос

## 🎯 Готово!

Теперь регистрация должна работать через Railway API!

---

## 📋 Чек-лист

- [ ] Railway URL скопирован из Railway Dashboard
- [ ] `js/config.js` обновлен с правильным URL
- [ ] `JWT_SECRET` установлен в Railway
- [ ] `NODE_ENV=production` установлен в Railway
- [ ] `DATABASE_URL` создан автоматически в Railway
- [ ] Изменения закоммичены и запушены
- [ ] Vercel задеплоил новую версию
- [ ] Регистрация работает

---

## 🐛 Если что-то не работает

1. Проверьте консоль браузера - там будут ошибки
2. Проверьте логи Railway - там будут ошибки сервера
3. Убедитесь, что Railway URL правильный (без `/api`)
4. Убедитесь, что сервер запущен (проверьте `/health` endpoint)

