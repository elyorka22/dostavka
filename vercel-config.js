// Конфигурация для Vercel
// Этот файл будет использоваться для инжекции переменных окружения в статические файлы

// В Vercel переменные окружения доступны только во время сборки
// Для статических сайтов нужно использовать другой подход

// Вариант 1: Создать config.js файл, который будет генерироваться при деплое
// Вариант 2: Использовать window объект в HTML (текущий подход)

// Для использования Railway API, добавьте в index.html перед другими скриптами:
/*
<script>
    window.RAILWAY_API_URL = 'https://ваш-railway-url.railway.app';
    window.API_MODE = 'api';
</script>
*/

// Или используйте этот файл для генерации конфигурации при деплое

