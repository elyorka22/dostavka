# Инструкция по деплою проекта

Этот проект готов к деплою на Vercel с использованием Firebase для хранения данных.

## 📋 Предварительные требования

1. Аккаунт в [Firebase](https://console.firebase.google.com/)
2. Аккаунт в [Vercel](https://vercel.com/)
3. Аккаунт в [Railway](https://railway.app/) (опционально, для бэкенда)

---

## 🔥 Настройка Firebase

### 1. Создание проекта Firebase

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Нажмите "Add project" (Добавить проект)
3. Введите название проекта (например: "ovoshchi-frukty")
4. Следуйте инструкциям для создания проекта

### 2. Настройка Authentication

1. В Firebase Console перейдите в **Authentication** > **Get started**
2. Включите **Email/Password** провайдер
3. Сохраните настройки

### 3. Настройка Firestore Database

1. Перейдите в **Firestore Database** > **Create database**
2. Выберите режим: **Start in test mode** (для начала)
3. Выберите регион (например: `us-central1`)
4. Нажмите **Enable**

### 4. Настройка Storage (для изображений)

1. Перейдите в **Storage** > **Get started**
2. Выберите режим: **Start in test mode**
3. Выберите регион (тот же, что и для Firestore)
4. Нажмите **Enable**

### 5. Получение конфигурации

1. Перейдите в **Project Settings** (⚙️) > **Your apps**
2. Нажмите на иконку **Web** (`</>`)
3. Зарегистрируйте приложение (например: "Web App")
4. Скопируйте конфигурацию Firebase

Вы получите объект вида:
```javascript
const firebaseConfig = {
  apiKey: "AIza...",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdef"
};
```

### 6. Настройка правил безопасности Firestore

Перейдите в **Firestore Database** > **Rules** и установите:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Правила для продуктов
    match /products/{productId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
    // Правила для категорий
    match /categories/{categoryId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
    // Правила для заказов
    match /orders/{orderId} {
      allow read, write: if request.auth != null;
    }
    
    // Правила для пользователей
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Правила для корзины
    match /cart/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

### 7. Настройка правил безопасности Storage

Перейдите в **Storage** > **Rules** и установите:

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
```

---

## 🚀 Деплой на Vercel

### Вариант 1: Деплой через GitHub

1. **Создайте репозиторий на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/yourusername/ovoshchi-frukty.git
   git push -u origin main
   ```

2. **Подключите проект к Vercel:**
   - Перейдите на [Vercel](https://vercel.com/)
   - Нажмите **Add New Project**
   - Импортируйте ваш GitHub репозиторий
   - Vercel автоматически определит настройки из `vercel.json`

3. **Настройте переменные окружения:**
   - В настройках проекта Vercel перейдите в **Settings** > **Environment Variables**
   - Добавьте все переменные из `env.example`:
     ```
     FIREBASE_API_KEY=ваш_api_key
     FIREBASE_AUTH_DOMAIN=ваш_auth_domain
     FIREBASE_PROJECT_ID=ваш_project_id
     FIREBASE_STORAGE_BUCKET=ваш_storage_bucket
     FIREBASE_MESSAGING_SENDER_ID=ваш_messaging_sender_id
     FIREBASE_APP_ID=ваш_app_id
     API_MODE=firebase
     ```

4. **Деплой:**
   - Vercel автоматически задеплоит проект
   - После деплоя получите URL вашего сайта

### Вариант 2: Деплой через Vercel CLI

1. **Установите Vercel CLI:**
   ```bash
   npm i -g vercel
   ```

2. **Войдите в Vercel:**
   ```bash
   vercel login
   ```

3. **Деплой:**
   ```bash
   vercel
   ```

4. **Настройте переменные окружения:**
   ```bash
   vercel env add FIREBASE_API_KEY
   vercel env add FIREBASE_AUTH_DOMAIN
   vercel env add FIREBASE_PROJECT_ID
   vercel env add FIREBASE_STORAGE_BUCKET
   vercel env add FIREBASE_MESSAGING_SENDER_ID
   vercel env add FIREBASE_APP_ID
   vercel env add API_MODE
   ```

5. **Продакшен деплой:**
   ```bash
   vercel --prod
   ```

---

## 🔧 Настройка Firebase в коде

### Обновление конфигурации Firebase

1. Откройте файл `js/firebase-config.js`
2. Замените значения в объекте `firebaseConfig` на ваши данные из Firebase Console
3. Или используйте переменные окружения (они автоматически подхватятся в Vercel)

### Переключение на Firebase

В консоли браузера или в коде выполните:

```javascript
// Переключить на Firebase
setAPIMode('firebase');
```

Или установите переменную окружения `API_MODE=firebase` в Vercel.

---

## 🚂 Деплой бэкенда на Railway (опционально)

Если вы хотите использовать собственный API вместо Firebase:

1. **Создайте Node.js API сервер** (например, Express.js)
2. **Подключите к Railway:**
   - Перейдите на [Railway](https://railway.app/)
   - Нажмите **New Project** > **Deploy from GitHub repo**
   - Выберите репозиторий с бэкендом

3. **Настройте переменные окружения в Railway:**
   - Добавьте все необходимые переменные окружения
   - Railway автоматически предоставит URL для вашего API

4. **Обновите конфигурацию:**
   - В Vercel добавьте переменную `API_BASE_URL` с URL вашего Railway API
   - Установите `API_MODE=api`

---

## 📝 Структура проекта

```
ovoshchi-frukty/
├── index.html              # Главная страница
├── login.html              # Страница входа
├── cart.html               # Страница корзины
├── checkout.html           # Оформление заказа
├── admin.html              # Админ-панель
├── manager.html            # Панель менеджера
├── picker.html             # Панель сборщика
├── courier.html            # Панель курьера
├── orders.html             # Страница заказов
├── js/                     # JavaScript модули
│   ├── api.js              # API модуль (поддерживает localStorage, Firebase, HTTP)
│   ├── firebase-config.js  # Конфигурация Firebase
│   └── ...
├── styles/                 # CSS стили
├── images/                 # Изображения
├── vercel.json             # Конфигурация Vercel
├── package.json            # Зависимости проекта
└── env.example             # Пример переменных окружения
```

---

## 🔐 Режимы работы API

Проект поддерживает три режима работы:

1. **localStorage** (по умолчанию) - данные хранятся в браузере
2. **firebase** - данные хранятся в Firebase Firestore
3. **api** - данные получаются через HTTP API

Переключение режима:
```javascript
setAPIMode('firebase'); // или 'localStorage', 'api'
```

---

## ✅ Чеклист перед деплоем

- [ ] Создан проект в Firebase
- [ ] Настроена Authentication (Email/Password)
- [ ] Создана база данных Firestore
- [ ] Настроен Storage для изображений
- [ ] Настроены правила безопасности Firestore
- [ ] Настроены правила безопасности Storage
- [ ] Скопирована конфигурация Firebase
- [ ] Добавлены переменные окружения в Vercel
- [ ] Обновлен `firebase-config.js` (если не используете переменные окружения)
- [ ] Установлен `API_MODE=firebase` в переменных окружения
- [ ] Проект задеплоен на Vercel
- [ ] Проверена работа сайта после деплоя

---

## 🐛 Решение проблем

### Firebase не инициализируется

1. Проверьте, что Firebase SDK загружен в HTML
2. Проверьте конфигурацию Firebase
3. Проверьте консоль браузера на наличие ошибок

### Данные не сохраняются

1. Проверьте правила безопасности Firestore
2. Проверьте, что пользователь авторизован (для операций записи)
3. Проверьте режим API: `API_MODE=firebase`

### Ошибки CORS

1. Убедитесь, что домен добавлен в настройки Firebase
2. В Firebase Console: **Authentication** > **Settings** > **Authorized domains**

---

## 📚 Дополнительные ресурсы

- [Firebase Documentation](https://firebase.google.com/docs)
- [Vercel Documentation](https://vercel.com/docs)
- [Railway Documentation](https://docs.railway.app/)

---

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет доступен по адресу, предоставленному Vercel.

