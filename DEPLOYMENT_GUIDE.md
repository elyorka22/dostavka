# Руководство по деплою на Vercel и Railway

## ✅ Проект загружен на GitHub

Репозиторий: https://github.com/elyorka22/dostavka.git

## 🚀 Деплой на Vercel

### Шаг 1: Подключение репозитория

1. Перейдите на [Vercel](https://vercel.com/)
2. Войдите в аккаунт (или создайте новый)
3. Нажмите **Add New Project**
4. Выберите репозиторий `elyorka22/dostavka`
5. Vercel автоматически определит настройки из `vercel.json`

### Шаг 2: Настройка переменных окружения

В настройках проекта Vercel перейдите в **Settings** > **Environment Variables** и добавьте:

```
FIREBASE_API_KEY=ваш_api_key
FIREBASE_AUTH_DOMAIN=ваш_auth_domain
FIREBASE_PROJECT_ID=ваш_project_id
FIREBASE_STORAGE_BUCKET=ваш_storage_bucket
FIREBASE_MESSAGING_SENDER_ID=ваш_messaging_sender_id
FIREBASE_APP_ID=ваш_app_id
API_MODE=firebase
```

### Шаг 3: Деплой

- Vercel автоматически задеплоит проект после подключения
- После деплоя вы получите URL вида: `https://dostavka-xxx.vercel.app`

### Важно для Vercel

- ✅ Все пути относительные (без `/` в начале)
- ✅ `vercel.json` настроен правильно
- ✅ Статические файлы будут обслуживаться корректно
- ✅ Нет проблем с root путями

## 🚂 Деплой на Railway

### Шаг 1: Подключение репозитория

1. Перейдите на [Railway](https://railway.app/)
2. Войдите в аккаунт (или создайте новый)
3. Нажмите **New Project**
4. Выберите **Deploy from GitHub repo**
5. Выберите репозиторий `elyorka22/dostavka`

### Шаг 2: Настройка

Railway автоматически определит настройки из:
- `railway.json` - основная конфигурация
- `nixpacks.toml` - конфигурация сборки

### Шаг 3: Настройка переменных окружения

В настройках проекта Railway перейдите в **Variables** и добавьте те же переменные, что и для Vercel.

### Шаг 4: Настройка порта

Railway автоматически установит переменную `PORT`. Python сервер будет использовать её.

### Важно для Railway

- ✅ `railway.json` настроен для статического сайта
- ✅ `nixpacks.toml` настроен для Python сервера
- ✅ Порт будет автоматически установлен через переменную `$PORT`

## 🔧 Проверка после деплоя

### Проверьте:

1. ✅ Главная страница загружается: `/index.html` или `/`
2. ✅ Все страницы доступны:
   - `/login.html`
   - `/cart.html`
   - `/checkout.html`
   - `/admin.html`
   - И т.д.
3. ✅ Статические ресурсы загружаются:
   - `/styles/*.css`
   - `/js/*.js`
   - `/images/**`
4. ✅ Firebase инициализируется корректно
5. ✅ Нет ошибок в консоли браузера

## 🐛 Решение проблем

### Проблема: 404 на страницах

**Решение:** Убедитесь, что `vercel.json` не содержит rewrite для всех путей (уже исправлено)

### Проблема: Статические файлы не загружаются

**Решение:** Проверьте, что пути относительные (без `/` в начале)

### Проблема: Firebase не работает

**Решение:** 
1. Проверьте переменные окружения
2. Убедитесь, что домен добавлен в Firebase Console
3. Проверьте консоль браузера на ошибки

### Проблема: Railway не запускается

**Решение:**
1. Проверьте логи в Railway
2. Убедитесь, что `PORT` переменная установлена
3. Проверьте `nixpacks.toml` конфигурацию

## 📝 Структура проекта

```
dostavka/
├── index.html              # Главная страница
├── login.html              # Страница входа
├── cart.html               # Корзина
├── checkout.html           # Оформление заказа
├── admin.html              # Админ-панель
├── js/                     # JavaScript модули
├── styles/                 # CSS стили
├── images/                 # Изображения
├── vercel.json             # Конфигурация Vercel
├── railway.json            # Конфигурация Railway
├── nixpacks.toml           # Конфигурация сборки Railway
├── package.json            # Метаданные проекта
└── env.example             # Пример переменных окружения
```

## ✅ Готово!

Проект полностью настроен и готов к деплою на Vercel и Railway.

