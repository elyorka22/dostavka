# 🔐 Как добавить супер-админа в Firebase

Эта инструкция поможет вам вручную создать супер-админа в Firebase Authentication и Firestore.

## 📋 Шаг 1: Создание пользователя в Firebase Authentication

### 1.1 Откройте Firebase Console

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Выберите ваш проект: **dostavka-27346**

### 1.2 Перейдите в Authentication

1. В левом меню нажмите **Authentication**
2. Перейдите на вкладку **Users** (Пользователи)

### 1.3 Добавьте нового пользователя

1. Нажмите кнопку **Add user** (Добавить пользователя)
2. Заполните форму:
   - **Email:** `admin@delivery.uz` (или ваш email)
   - **Password:** `admin123` (или ваш пароль)
   - **Password confirmation:** повторите пароль
3. Нажмите **Add user** (Добавить пользователя)

### 1.4 Скопируйте UID пользователя

1. После создания пользователя найдите его в списке
2. Нажмите на пользователя, чтобы открыть детали
3. **Скопируйте UID** (уникальный идентификатор пользователя)
   - Это строка вида: `abc123def456ghi789...`
   - Она понадобится на следующем шаге

**✅ Шаг 1 завершен!** Пользователь создан в Firebase Authentication.

---

## 📝 Шаг 2: Создание документа пользователя в Firestore

### 2.1 Перейдите в Firestore Database

1. В левом меню Firebase Console нажмите **Firestore Database**
2. Перейдите на вкладку **Data** (Данные)

### 2.2 Создайте коллекцию users (если её нет)

1. Если коллекции `users` нет:
   - Нажмите **Start collection** (Начать коллекцию)
   - Collection ID: `users`
   - Нажмите **Next**

### 2.3 Создайте документ для супер-админа

1. Нажмите **Add document** (Добавить документ) в коллекции `users`
2. **Document ID:** вставьте **UID** пользователя из шага 1.4
   - ⚠️ **Важно:** Document ID должен совпадать с UID из Firebase Authentication!
3. Нажмите **Next**

### 2.4 Добавьте поля документа

Добавьте следующие поля (нажмите **Add field** для каждого):

| Field | Type | Value |
|-------|------|-------|
| `name` | string | `Super-admin` |
| `email` | string | `admin@delivery.uz` (или ваш email) |
| `role` | string | `super-admin` |
| `phone` | string | `+998901234567` (опционально) |
| `createdAt` | timestamp | Нажмите на иконку часов и выберите текущее время |

**Пример структуры документа:**

```json
{
  "name": "Super-admin",
  "email": "admin@delivery.uz",
  "role": "super-admin",
  "phone": "+998901234567",
  "createdAt": "2026-01-08T10:00:00Z"
}
```

4. Нажмите **Save** (Сохранить)

**✅ Шаг 2 завершен!** Документ пользователя создан в Firestore.

---

## 🔍 Шаг 3: Проверка

### 3.1 Проверьте в Authentication

1. Перейдите в **Authentication** > **Users**
2. Убедитесь, что пользователь создан
3. Email должен совпадать с тем, что вы указали

### 3.2 Проверьте в Firestore

1. Перейдите в **Firestore Database** > **Data**
2. Откройте коллекцию `users`
3. Найдите документ с UID вашего пользователя
4. Убедитесь, что поле `role` равно `super-admin`

### 3.3 Проверьте вход на сайте

1. Откройте ваш сайт
2. Перейдите на страницу входа
3. Войдите с учетными данными:
   - **Email:** `admin@delivery.uz`
   - **Password:** `admin123`
4. После входа вы должны быть перенаправлены на `admin.html`
5. Проверьте, что у вас есть доступ к админ-панели

**✅ Шаг 3 завершен!** Супер-админ создан и работает!

---

## 🛠️ Альтернативный способ: Использование Firebase Admin SDK

Если у вас есть доступ к серверу или вы хотите автоматизировать процесс, можно использовать Firebase Admin SDK:

### Пример кода (Node.js):

```javascript
const admin = require('firebase-admin');

// Инициализация (нужен service account key)
admin.initializeApp({
  credential: admin.credential.cert(serviceAccount)
});

// Создание пользователя в Authentication
admin.auth().createUser({
  email: 'admin@delivery.uz',
  password: 'admin123',
  displayName: 'Super-admin'
})
.then(function(userRecord) {
  console.log('Пользователь создан:', userRecord.uid);
  
  // Создание документа в Firestore
  return admin.firestore().collection('users').doc(userRecord.uid).set({
    name: 'Super-admin',
    email: 'admin@delivery.uz',
    role: 'super-admin',
    phone: '+998901234567',
    createdAt: admin.firestore.FieldValue.serverTimestamp()
  });
})
.then(function() {
  console.log('Документ пользователя создан в Firestore');
})
.catch(function(error) {
  console.error('Ошибка:', error);
});
```

---

## 📌 Важные замечания

1. **UID должен совпадать:**
   - Document ID в Firestore должен быть равен UID из Firebase Authentication
   - Это критически важно для правильной работы системы

2. **Роль super-admin:**
   - Роль должна быть точно `super-admin` (с дефисом)
   - Регистр важен: `super-admin`, а не `Super-Admin` или `SUPER_ADMIN`

3. **Безопасность:**
   - Используйте надежный пароль в продакшене
   - Не храните пароли в коде или документации
   - Регулярно меняйте пароли администраторов

4. **Проверка правил Firestore:**
   - Убедитесь, что правила Firestore позволяют чтение документов пользователей
   - Если правила слишком строгие, временно ослабьте их для создания админа

---

## 🔧 Создание других ролей

Аналогичным образом можно создать пользователей с другими ролями:

| Роль | Значение поля `role` |
|------|---------------------|
| Super-admin | `super-admin` |
| Manager | `manager` |
| Picker | `picker` |
| Courier | `courier` |
| Customer | `customer` |

Просто замените значение поля `role` на нужное при создании документа в Firestore.

---

## ❓ Решение проблем

### Проблема: Пользователь не может войти

**Решение:**
1. Проверьте, что пользователь создан в Firebase Authentication
2. Проверьте, что email и пароль правильные
3. Проверьте, что документ создан в Firestore с правильным UID
4. Проверьте, что поле `role` установлено правильно

### Проблема: Пользователь не имеет прав администратора

**Решение:**
1. Проверьте, что в Firestore поле `role` равно `super-admin`
2. Проверьте, что Document ID совпадает с UID из Authentication
3. Очистите кеш браузера и попробуйте войти снова

### Проблема: Ошибка при создании документа в Firestore

**Решение:**
1. Проверьте правила безопасности Firestore
2. Убедитесь, что у вас есть права на запись
3. Попробуйте создать документ через Firebase Console

---

**✅ Готово!** Теперь у вас есть супер-админ в Firebase!

