# Инструкция по загрузке проекта в Git

## ✅ Git репозиторий инициализирован

Проект уже инициализирован и готов к загрузке на GitHub.

## 🚀 Загрузка на GitHub

### Вариант 1: Через GitHub веб-интерфейс

1. **Создайте новый репозиторий на GitHub:**
   - Перейдите на [GitHub](https://github.com)
   - Нажмите **New repository**
   - Введите название (например: `ovoshchi-frukty`)
   - Выберите **Public** или **Private**
   - **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
   - Нажмите **Create repository**

2. **Подключите локальный репозиторий к GitHub:**
   ```bash
   git remote add origin https://github.com/ВАШ_USERNAME/ovoshchi-frukty.git
   git push -u origin main
   ```

### Вариант 2: Через GitHub CLI

Если у вас установлен GitHub CLI:

```bash
gh repo create ovoshchi-frukty --public --source=. --remote=origin --push
```

### Вариант 3: Через SSH

Если вы используете SSH ключи:

```bash
git remote add origin git@github.com:ВАШ_USERNAME/ovoshchi-frukty.git
git push -u origin main
```

## 📝 Команды для загрузки

Выполните следующие команды в терминале:

```bash
# Добавить remote репозиторий (замените URL на ваш)
git remote add origin https://github.com/ВАШ_USERNAME/ovoshchi-frukty.git

# Загрузить код на GitHub
git push -u origin main
```

## 🔄 Обновление кода в будущем

После внесения изменений:

```bash
# Проверить изменения
git status

# Добавить все изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Загрузить на GitHub
git push
```

## 📋 Текущий статус

- ✅ Git репозиторий инициализирован
- ✅ Все файлы добавлены в репозиторий
- ✅ Создан первый коммит
- ✅ Ветка переименована в `main`
- ⏳ Ожидается добавление remote и push на GitHub

## 🔐 Важно

- Не загружайте файл `.env` с реальными ключами (он уже в .gitignore)
- Используйте `env.example` как шаблон
- Все секретные данные должны быть в переменных окружения Vercel

## 🎉 Готово!

После выполнения команд ваш проект будет доступен на GitHub и готов к деплою на Vercel!

