# 🚀 Настройка полноценного сервиса доставки

## 📋 Что нужно подключить

Для полноценного сервиса доставки продуктов с БД, корзиной и админ-панелями:

### ✅ ОБЯЗАТЕЛЬНО:
1. **Firebase** - База данных, аутентификация, хранение файлов
2. **Vercel** - Хостинг фронтенда

### ⚠️ ОПЦИОНАЛЬНО:
3. **Railway** - Только если понадобится свой бэкенд API

---

## 🔥 ШАГ 1: Настройка Firebase (ОБЯЗАТЕЛЬНО)

Firebase будет хранить все данные вашего сервиса.

### 1.1 Создание проекта

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Нажмите **Add project** (Добавить проект)
3. Введите название: `dostavka` или `ovoshchi-frukty`
4. Следуйте инструкциям

### 1.2 Настройка Firestore Database (База данных)

1. В Firebase Console перейдите в **Firestore Database**
2. Нажмите **Create database**
3. Выберите режим: **Start in production mode** (для продакшена)
4. Выберите регион: `us-central1` или ближайший к вам
5. Нажмите **Enable**

**Структура данных:**
```
Firestore/
├── products/          # Товары
│   └── {productId}/
│       ├── name
│       ├── price
│       ├── category
│       └── ...
├── categories/        # Категории
│   └── {categoryId}/
├── orders/           # Заказы
│   └── {orderId}/
│       ├── customerName
│       ├── items
│       ├── status
│       └── ...
├── users/            # Пользователи
│   └── {userId}/
│       ├── role
│       ├── name
│       └── ...
└── cart/             # Корзины (по userId)
    └── {userId}/
        └── items[]
```

### 1.3 Настройка Authentication (Аутентификация)

1. Перейдите в **Authentication** > **Get started**
2. Включите **Email/Password** провайдер
3. Нажмите **Save**

**Роли пользователей:**
- Super-admin
- Manager
- Picker
- Courier
- Customer

### 1.4 Настройка Storage (Хранение файлов)

1. Перейдите в **Storage** > **Get started**
2. Выберите режим: **Start in production mode**
3. Выберите регион (тот же, что и Firestore)
4. Нажмите **Enable**

**Структура:**
```
Storage/
├── categories/       # Изображения категорий
│   └── {categoryId}.jpg
└── products/         # Изображения товаров
    └── {productId}.jpg
```

### 1.5 Правила безопасности Firestore

Перейдите в **Firestore Database** > **Rules** и установите:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Товары - все могут читать, только авторизованные писать
    match /products/{productId} {
      allow read: if true;
      allow write: if request.auth != null && 
                      get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role in ['super-admin', 'manager'];
    }
    
    // Категории - все могут читать, только админы писать
    match /categories/{categoryId} {
      allow read: if true;
      allow write: if request.auth != null && 
                      get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'super-admin';
    }
    
    // Заказы - только авторизованные
    match /orders/{orderId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null;
      allow update: if request.auth != null && (
        // Пользователь может обновлять только свои заказы
        resource.data.customerId == request.auth.uid ||
        // Или админы/менеджеры
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role in ['super-admin', 'manager', 'picker', 'courier']
      );
    }
    
    // Пользователи - только свои данные
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Корзина - только свой пользователь
    match /cart/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

### 1.6 Правила безопасности Storage

Перейдите в **Storage** > **Rules**:

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Категории - все могут читать, только админы писать
    match /categories/{categoryId} {
      allow read: if true;
      allow write: if request.auth != null && 
                      firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role == 'super-admin';
    }
    
    // Товары - все могут читать, только админы/менеджеры писать
    match /products/{productId} {
      allow read: if true;
      allow write: if request.auth != null && 
                      firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role in ['super-admin', 'manager'];
    }
  }
}
```

### 1.7 Получение конфигурации

1. Перейдите в **Project Settings** (⚙️) > **Your apps**
2. Нажмите на иконку **Web** (`</>`)
3. Зарегистрируйте приложение: `Web App`
4. Скопируйте конфигурацию:

```javascript
const firebaseConfig = {
  apiKey: "AIza...",
  authDomain: "ваш-проект.firebaseapp.com",
  projectId: "ваш-проект-id",
  storageBucket: "ваш-проект.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdef"
};
```

### 1.8 Добавление домена в Firebase

1. В **Authentication** > **Settings** > **Authorized domains**
2. Добавьте домен Vercel (будет после деплоя)
3. Например: `dostavka.vercel.app`

---

## 🚀 ШАГ 2: Настройка Vercel (ОБЯЗАТЕЛЬНО)

Vercel будет хостить ваш фронтенд.

### 2.1 Подключение репозитория

1. Перейдите на [Vercel](https://vercel.com/)
2. Войдите через GitHub
3. Нажмите **Add New Project**
4. Выберите репозиторий `elyorka22/dostavka`
5. Vercel автоматически определит настройки

### 2.2 Настройка переменных окружения

В настройках проекта Vercel перейдите в **Settings** > **Environment Variables**:

```
FIREBASE_API_KEY=ваш_api_key
FIREBASE_AUTH_DOMAIN=ваш_auth_domain
FIREBASE_PROJECT_ID=ваш_project_id
FIREBASE_STORAGE_BUCKET=ваш_storage_bucket
FIREBASE_MESSAGING_SENDER_ID=ваш_messaging_sender_id
FIREBASE_APP_ID=ваш_app_id
API_MODE=firebase
```

### 2.3 Деплой

- Vercel автоматически задеплоит проект
- После деплоя получите URL: `https://dostavka-xxx.vercel.app`

### 2.4 Настройка домена

1. В Vercel перейдите в **Settings** > **Domains**
2. Добавьте свой домен или используйте бесплатный `.vercel.app`
3. SSL настроится автоматически

---

## 🚂 ШАГ 3: Railway (ОПЦИОНАЛЬНО)

Railway нужен **ТОЛЬКО** если:
- Нужен собственный бэкенд API
- Сложная бизнес-логика
- Интеграции с платежными системами

### Если НЕ нужен Railway:

✅ **Можно не подключать** - Firebase Functions достаточно для большинства задач

### Если нужен Railway:

1. Создайте Node.js API сервер
2. Подключите к Firebase
3. Задеплойте на Railway
4. Используйте для сложной логики

---

## 🔧 ШАГ 4: Обновление кода

### 4.1 Обновление Firebase конфигурации

Откройте `js/firebase-config.js` и замените значения на свои из Firebase Console.

Или используйте переменные окружения (они автоматически подхватятся в Vercel).

### 4.2 Переключение на Firebase

В Vercel установите переменную:
```
API_MODE=firebase
```

Или в коде:
```javascript
setAPIMode('firebase');
```

---

## ✅ Чеклист настройки

### Firebase:
- [ ] Проект создан
- [ ] Firestore Database настроена
- [ ] Authentication настроена (Email/Password)
- [ ] Storage настроен
- [ ] Правила безопасности Firestore установлены
- [ ] Правила безопасности Storage установлены
- [ ] Конфигурация получена
- [ ] Домен Vercel добавлен в Authorized domains

### Vercel:
- [ ] Репозиторий подключен
- [ ] Переменные окружения добавлены
- [ ] Проект задеплоен
- [ ] Домен настроен
- [ ] SSL работает

### Код:
- [ ] `API_MODE=firebase` установлен
- [ ] Firebase инициализируется
- [ ] Данные сохраняются в Firestore
- [ ] Аутентификация работает
- [ ] Загрузка файлов работает

---

## 🎯 Итоговая архитектура

```
Пользователи
    ↓
Vercel (Фронтенд)
    ↓
Firebase
    ├── Firestore (БД)
    ├── Auth (Вход)
    ├── Storage (Файлы)
    └── Functions (Логика)
```

**Railway не нужен** для базового функционала!

---

## 💰 Стоимость

### Бесплатные тарифы (достаточно для старта):

**Firebase:**
- Firestore: 50K чтений/день, 20K записей/день
- Auth: 10K активных пользователей
- Storage: 5GB, 1GB/день трафика

**Vercel:**
- 100GB bandwidth/месяц
- Бесплатный SSL и CDN

**Итого:** Бесплатно для старта! 🎉

---

## 🎉 Готово!

После выполнения всех шагов ваш полноценный сервис доставки будет работать с:
- ✅ Базой данных (Firebase Firestore)
- ✅ Аутентификацией (Firebase Auth)
- ✅ Хранением файлов (Firebase Storage)
- ✅ Быстрым фронтендом (Vercel CDN)
- ✅ Всеми функциями (корзина, админ-панели, заказы)

**Railway не обязателен** - Firebase + Vercel достаточно!

