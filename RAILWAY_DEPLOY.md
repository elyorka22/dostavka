# 🚂 Подробная инструкция по деплою на Railway

## 📋 Что такое Railway?

Railway - это платформа для деплоя приложений, которая автоматически определяет тип проекта и настраивает его.

## 🎯 Настройка Root директории

**Важно:** В нашем проекте все файлы находятся в корневой директории (root), поэтому настройка очень простая.

### Структура проекта:
```
dostavka/                    ← Это root директория
├── index.html               ← Главная страница
├── login.html
├── cart.html
├── js/                      ← JavaScript файлы
├── styles/                  ← CSS файлы
├── images/                  ← Изображения
├── railway.json             ← Конфигурация Railway
└── nixpacks.toml           ← Конфигурация сборки
```

**Root директория = корень проекта** (где находится `index.html`)

## 🚀 Пошаговая инструкция по деплою

### Шаг 1: Создание аккаунта на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите **Start a New Project** или **Login**
3. Войдите через GitHub (рекомендуется)

### Шаг 2: Подключение репозитория

1. После входа нажмите **New Project**
2. Выберите **Deploy from GitHub repo**
3. Найдите репозиторий `elyorka22/dostavka`
4. Нажмите на него
5. Railway автоматически начнет деплой

### Шаг 3: Настройка Root директории

**Railway автоматически определит root**, но если нужно настроить вручную:

1. В проекте Railway откройте **Settings**
2. Найдите раздел **Root Directory**
3. Оставьте пустым (по умолчанию - корень проекта) или укажите `.`
4. Сохраните

**Важно:** Для нашего проекта root должен быть `.` (точка) или пусто - это означает корень проекта.

### Шаг 4: Настройка переменных окружения

1. В проекте Railway откройте **Variables**
2. Добавьте следующие переменные:

```
FIREBASE_API_KEY=ваш_api_key
FIREBASE_AUTH_DOMAIN=ваш_auth_domain
FIREBASE_PROJECT_ID=ваш_project_id
FIREBASE_STORAGE_BUCKET=ваш_storage_bucket
FIREBASE_MESSAGING_SENDER_ID=ваш_messaging_sender_id
FIREBASE_APP_ID=ваш_app_id
API_MODE=firebase
```

3. Нажмите **Add** для каждой переменной

### Шаг 5: Настройка порта

**Railway автоматически устанавливает переменную `PORT`**, поэтому ничего делать не нужно.

Python сервер будет использовать эту переменную:
```bash
python3 -m http.server $PORT --directory .
```

### Шаг 6: Проверка деплоя

1. Railway автоматически начнет деплой
2. Дождитесь завершения (обычно 1-2 минуты)
3. После успешного деплоя вы получите URL вида: `https://dostavka-production.up.railway.app`

### Шаг 7: Настройка домена (опционально)

1. В проекте Railway откройте **Settings** > **Domains**
2. Нажмите **Generate Domain** или добавьте свой домен
3. Railway автоматически настроит SSL сертификат

## 🔧 Конфигурационные файлы

### railway.json

```json
{
  "deploy": {
    "startCommand": "python3 -m http.server $PORT --directory ."
  }
}
```

- `--directory .` - указывает, что сервер должен работать из текущей директории (root проекта)

### nixpacks.toml

```toml
[start]
cmd = "python3 -m http.server $PORT --directory ."
```

- Этот файл говорит Railway, как запустить приложение
- `--directory .` - root директория проекта

## ✅ Проверка после деплоя

### 1. Проверьте главную страницу:
```
https://ваш-домен.railway.app/
https://ваш-домен.railway.app/index.html
```

### 2. Проверьте другие страницы:
```
https://ваш-домен.railway.app/login.html
https://ваш-домен.railway.app/cart.html
https://ваш-домен.railway.app/admin.html
```

### 3. Проверьте статические файлы:
```
https://ваш-домен.railway.app/styles/base.css
https://ваш-домен.railway.app/js/api.js
https://ваш-домен.railway.app/images/categories/photo_2026-01-08_05-08-24.jpg
```

## 🐛 Решение проблем

### Проблема: "Cannot find index.html"

**Причина:** Root директория настроена неправильно

**Решение:**
1. В Railway Settings > Root Directory установите `.` или оставьте пустым
2. Перезапустите деплой

### Проблема: "Port already in use"

**Причина:** Порт не установлен правильно

**Решение:**
- Railway автоматически устанавливает `$PORT`
- Убедитесь, что в команде используется `$PORT`, а не конкретное число

### Проблема: Статические файлы не загружаются

**Причина:** Неправильный путь к файлам

**Решение:**
- Убедитесь, что все пути относительные (без `/` в начале)
- Проверьте, что файлы находятся в правильных папках

### Проблема: "Python not found"

**Причина:** Python не установлен в контейнере

**Решение:**
- `nixpacks.toml` уже настроен для установки Python
- Railway автоматически установит Python из конфигурации

## 📊 Мониторинг

### Просмотр логов:

1. В проекте Railway откройте **Deployments**
2. Выберите последний деплой
3. Нажмите **View Logs**

### Метрики:

- Railway автоматически показывает использование CPU, памяти, сети
- Доступны в разделе **Metrics**

## 🔄 Обновление проекта

После каждого push в GitHub:

1. Railway автоматически обнаружит изменения
2. Начнет новый деплой
3. После завершения обновит сайт

Или вручную:

1. В Railway откройте проект
2. Нажмите **Redeploy**

## 💡 Полезные советы

1. **Root директория:** Всегда `.` для нашего проекта (корень)
2. **Порт:** Всегда используйте `$PORT` (Railway автоматически установит)
3. **Переменные окружения:** Добавляйте через интерфейс Railway, не в коде
4. **Логи:** Проверяйте логи при проблемах
5. **Домен:** Railway предоставляет бесплатный домен с SSL

## ✅ Чеклист деплоя

- [ ] Создан аккаунт на Railway
- [ ] Подключен репозиторий GitHub
- [ ] Root директория настроена (`.` или пусто)
- [ ] Добавлены переменные окружения
- [ ] Деплой завершен успешно
- [ ] Главная страница открывается
- [ ] Все страницы доступны
- [ ] Статические файлы загружаются
- [ ] Firebase работает
- [ ] Домен настроен (опционально)

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет доступен на Railway!

**URL будет:** `https://ваш-проект.up.railway.app`

