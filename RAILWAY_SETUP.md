# 🚂 Настройка Railway для регистрации

## Шаг 1: Создание проекта на Railway

1. Перейдите на [Railway](https://railway.app/)
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий `dostavka`

## Шаг 2: Настройка базы данных

1. В проекте Railway нажмите **"New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст PostgreSQL базу данных
4. Скопируйте **DATABASE_URL** из переменных окружения

## Шаг 3: Настройка переменных окружения

В Railway добавьте следующие переменные:

```
PORT=3000
JWT_SECRET=ваш-секретный-ключ-измените-это
NODE_ENV=production
DATABASE_URL=автоматически-установлен-railway
```

## Шаг 4: Деплой

1. Railway автоматически обнаружит `package.json`
2. Установит зависимости
3. Запустит `npm start`
4. Сервер будет доступен по URL, который Railway предоставит

## Шаг 5: Обновление фронтенда

### Изменить `js/api.js`:

```javascript
// Базовый URL API
var API_BASE_URL = 'https://ваш-railway-url.railway.app/api';

// Режим работы
var API_MODE = 'api'; // Использовать Railway API
```

### Добавить функцию регистрации через API:

```javascript
// В js/users.js добавить поддержку API режима
function register(name, email, password, phone) {
    if (API_MODE === 'api') {
        return fetch(API_BASE_URL + '/auth/register', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({ name, email, password, phone })
        })
        .then(response => response.json())
        .then(data => {
            if (data.success) {
                // Сохранить токен
                localStorage.setItem('authToken', data.token);
                // Сохранить пользователя
                usersStorage.currentUser = data.user;
                saveCurrentUser();
                return data.user;
            } else {
                throw new Error(data.error);
            }
        });
    }
    // ... остальной код для Firebase
}
```

## Шаг 6: Проверка

1. Откройте сайт
2. Попробуйте зарегистрироваться
3. Проверьте, что пользователь создан в PostgreSQL
4. Проверьте, что токен сохранен в localStorage

## ✅ Преимущества Railway

- ✅ Полный контроль над бэкендом
- ✅ Легко отлаживать (логи в Railway)
- ✅ Не зависит от Firebase
- ✅ Можно добавить свою логику
- ✅ Бесплатный тариф для начала

## 🔧 Отладка

Если что-то не работает:

1. Проверьте логи в Railway Dashboard
2. Проверьте, что DATABASE_URL установлен
3. Проверьте, что сервер запущен (Health check: `/health`)
4. Проверьте CORS настройки

## 📝 Следующие шаги

После настройки Railway можно:
- Добавить больше endpoints
- Добавить валидацию
- Добавить rate limiting
- Добавить логирование
- И т.д.

