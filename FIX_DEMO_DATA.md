# 🔧 Исправление проблемы с демо данными

## ✅ Что исправлено

1. **Демо данные не создаются в продакшене** - только на localhost
2. **Автоматическое переключение на Firebase** - в продакшене всегда используется Firebase
3. **Очистка localStorage** - в продакшене localStorage очищается
4. **Правильная загрузка данных** - данные загружаются из Firebase перед отображением

## 🎯 Почему все еще видны демо карточки?

### Причина 1: Данные еще не добавлены в Firebase

**Решение:** Нужно добавить данные в Firebase!

#### Способ 1: Через админ-панель (рекомендуется)

1. Откройте сайт на Vercel
2. Войдите как admin (admin / admin123)
3. Перейдите в админ-панель
4. Добавьте товары и категории
5. Данные сохранятся в Firebase

#### Способ 2: Через Firebase Console

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект `dostavka-27346`
3. Перейдите в **Firestore Database**
4. Создайте коллекцию `products`
5. Добавьте товары вручную

#### Способ 3: Через консоль браузера

1. Откройте сайт на Vercel
2. Откройте консоль (F12)
3. Выполните:

```javascript
// Убедиться, что Firebase работает
initFirebase();
setAPIMode('firebase');

// Добавить товары
var products = [
    {
        name: 'Pomidor cherri',
        category: 'vegetables',
        weight: '250 g',
        unit: 'упак',
        price: 18900,
        badge: 'Hit',
        image: 'placeholder'
    },
    {
        name: 'Banan',
        category: 'fruits',
        weight: '1 kg',
        unit: 'кг',
        price: 8900,
        badge: '',
        image: 'placeholder'
    }
    // ... добавьте остальные
];

for (var i = 0; i < products.length; i++) {
    ProductsAPI.create(products[i]).then(function(result) {
        console.log('Товар добавлен:', result);
    });
}
```

### Причина 2: Кэш браузера

**Решение:** Очистите кэш браузера:

1. Нажмите `Ctrl+Shift+R` (Windows/Linux) или `Cmd+Shift+R` (Mac)
2. Или откройте в режиме инкогнито

### Причина 3: Старые данные в localStorage

**Решение:** Код теперь автоматически очищает localStorage в продакшене, но можно очистить вручную:

1. Откройте консоль (F12)
2. Выполните:
```javascript
localStorage.clear();
location.reload();
```

## ✅ Проверка

После добавления данных в Firebase:

1. **Перезагрузите страницу** (Ctrl+Shift+R)
2. **Откройте консоль** (F12)
3. **Проверьте:**
```javascript
// Должно быть true
isFirebaseInitialized();

// Должно быть 'firebase'
API_MODE;

// Должны быть товары из Firebase
getAllProducts();
```

4. **Проверьте Firebase Console:**
   - Откройте Firestore Database
   - Должна быть коллекция `products` с товарами

## 🎯 Итог

**Проблема:** Демо карточки показываются, потому что в Firebase еще нет данных.

**Решение:** Добавьте данные в Firebase через админ-панель или вручную.

После добавления данных демо карточки исчезнут автоматически!

