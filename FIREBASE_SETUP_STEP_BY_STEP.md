# 🔥 Интеграция Firebase - Пошаговая инструкция

## 📋 Шаг 1: Создание проекта в Firebase

### 1.1 Откройте Firebase Console

1. Перейдите на [https://console.firebase.google.com/](https://console.firebase.google.com/)
2. Войдите через Google аккаунт (или создайте новый)

### 1.2 Создайте новый проект

1. Нажмите кнопку **"Add project"** (Добавить проект) или **"Create a project"**
2. Введите название проекта: `dostavka` или `ovoshchi-frukty`
3. Нажмите **Continue** (Продолжить)

### 1.3 Настройка Google Analytics (опционально)

1. Firebase предложит включить Google Analytics
2. **Рекомендую включить** (можно отключить позже)
3. Выберите аккаунт Google Analytics или создайте новый
4. Нажмите **Continue**

### 1.4 Завершение создания проекта

1. Дождитесь создания проекта (обычно 30-60 секунд)
2. Нажмите **Continue**
3. Вы попадете в Firebase Console вашего проекта

**✅ Шаг 1 завершен!** Проект создан.

---

## 📊 Шаг 2: Настройка Firestore Database (База данных)

### 2.1 Создание базы данных

1. В левом меню Firebase Console найдите **Firestore Database**
2. Нажмите на него
3. Нажмите кнопку **"Create database"** (Создать базу данных)

### 2.2 Выбор режима безопасности

**Важно:** Выберите режим в зависимости от ваших потребностей:

**Вариант A: Production mode (рекомендуется для продакшена)**
- Более безопасный
- Требует настройки правил безопасности
- Выберите этот вариант

**Вариант B: Test mode (для тестирования)**
- Менее безопасный
- Доступ открыт на 30 дней
- Только для разработки

**Рекомендация:** Выберите **"Start in production mode"**

### 2.3 Выбор региона

1. Выберите регион для хранения данных:
   - **us-central1** (США) - быстрый, универсальный
   - **europe-west1** (Европа) - если пользователи в Европе
   - **asia-southeast1** (Азия) - если пользователи в Азии

2. **Рекомендация:** Выберите ближайший к вашим пользователям регион

3. Нажмите **Enable** (Включить)

### 2.4 Ожидание создания

1. Дождитесь создания базы данных (обычно 1-2 минуты)
2. После создания вы увидите пустую базу данных

**✅ Шаг 2 завершен!** База данных создана.

---

## 🔐 Шаг 3: Настройка Authentication (Аутентификация)

### 3.1 Включение Authentication

1. В левом меню найдите **Authentication**
2. Нажмите на него
3. Нажмите кнопку **"Get started"** (Начать)

### 3.2 Настройка Email/Password провайдера

1. В разделе **Sign-in method** (Методы входа) найдите **Email/Password**
2. Нажмите на него
3. Переключите переключатель **Enable** в положение **ON** (Включено)
4. Оставьте **Email link (passwordless sign-in)** выключенным (для начала)
5. Нажмите **Save** (Сохранить)

### 3.3 Проверка

1. Убедитесь, что **Email/Password** теперь показывает статус **Enabled** (Включено)

**✅ Шаг 3 завершен!** Аутентификация настроена.

---

## 📦 Шаг 4: Настройка Storage (Хранение файлов)

### 4.1 Включение Storage

1. В левом меню найдите **Storage**
2. Нажмите на него
3. Нажмите кнопку **"Get started"** (Начать)

### 4.2 Настройка правил безопасности

1. Выберите режим:
   - **Start in production mode** (рекомендуется)
   - Или **Start in test mode** (для тестирования)

2. **Рекомендация:** Выберите **"Start in production mode"**

### 4.3 Выбор региона

1. Выберите тот же регион, что и для Firestore
2. Нажмите **Done** (Готово)

### 4.4 Ожидание создания

1. Дождитесь создания Storage (обычно 30-60 секунд)

**✅ Шаг 4 завершен!** Storage настроен.

---

## 🔒 Шаг 5: Настройка правил безопасности Firestore

### 5.1 Открытие правил

1. Перейдите в **Firestore Database**
2. Откройте вкладку **Rules** (Правила)

### 5.2 Установка правил

Скопируйте и вставьте следующие правила:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Функция для проверки роли пользователя
    function isAdmin() {
      return request.auth != null && 
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'super-admin';
    }
    
    function isManager() {
      return request.auth != null && 
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role in ['super-admin', 'manager'];
    }
    
    // Товары - все могут читать, только админы/менеджеры писать
    match /products/{productId} {
      allow read: if true;
      allow create, update, delete: if isManager();
    }
    
    // Категории - все могут читать, только админы писать
    match /categories/{categoryId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }
    
    // Заказы
    match /orders/{orderId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null;
      allow update: if request.auth != null && (
        // Пользователь может обновлять только свои заказы
        resource.data.customerId == request.auth.uid ||
        // Или админы/менеджеры/сборщики/курьеры
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role in 
        ['super-admin', 'manager', 'picker', 'courier']
      );
    }
    
    // Пользователи - только свои данные
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Корзина - только свой пользователь
    match /cart/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

### 5.3 Сохранение правил

1. Нажмите кнопку **Publish** (Опубликовать)
2. Дождитесь подтверждения

**✅ Шаг 5 завершен!** Правила безопасности установлены.

---

## 🔒 Шаг 6: Настройка правил безопасности Storage

### 6.1 Открытие правил

1. Перейдите в **Storage**
2. Откройте вкладку **Rules** (Правила)

### 6.2 Установка правил

Скопируйте и вставьте следующие правила:

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Функция для проверки роли
    function isAdmin() {
      return request.auth != null && 
             firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role == 'super-admin';
    }
    
    function isManager() {
      return request.auth != null && 
             firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role in ['super-admin', 'manager'];
    }
    
    // Категории - все могут читать, только админы писать
    match /categories/{categoryId} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    // Товары - все могут читать, только админы/менеджеры писать
    match /products/{productId} {
      allow read: if true;
      allow write: if isManager();
    }
  }
}
```

### 6.3 Сохранение правил

1. Нажмите кнопку **Publish** (Опубликовать)
2. Дождитесь подтверждения

**✅ Шаг 6 завершен!** Правила Storage установлены.

---

## 🔑 Шаг 7: Получение конфигурации Firebase

### 7.1 Открытие настроек проекта

1. В левом верхнем углу нажмите на иконку **⚙️** (Settings)
2. Выберите **Project settings** (Настройки проекта)

### 7.2 Добавление веб-приложения

1. Прокрутите вниз до раздела **Your apps** (Ваши приложения)
2. Нажмите на иконку **Web** (`</>`)
3. Введите название приложения: `Web App` или `Dostavka Web`
4. **НЕ** устанавливайте Firebase Hosting (мы используем Vercel)
5. Нажмите **Register app** (Зарегистрировать приложение)

### 7.3 Копирование конфигурации

Вы увидите объект конфигурации:

```javascript
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "ваш-проект.firebaseapp.com",
  projectId: "ваш-проект-id",
  storageBucket: "ваш-проект.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdef123456"
};
```

**Важно:** Скопируйте эти значения! Они понадобятся в следующем шаге.

### 7.4 Продолжение

1. Нажмите **Continue to console** (Продолжить в консоли)

**✅ Шаг 7 завершен!** Конфигурация получена.

---

## 💻 Шаг 8: Обновление кода проекта

### 8.1 Открытие файла конфигурации

Откройте файл: `js/firebase-config.js`

### 8.2 Обновление конфигурации

Найдите функцию `getFirebaseConfig()` и замените значения по умолчанию:

```javascript
// Значения по умолчанию (замените на свои)
return {
    apiKey: "ВАШ_API_KEY",  // ← Вставьте сюда
    authDomain: "ВАШ_AUTH_DOMAIN",  // ← Вставьте сюда
    projectId: "ВАШ_PROJECT_ID",  // ← Вставьте сюда
    storageBucket: "ВАШ_STORAGE_BUCKET",  // ← Вставьте сюда
    messagingSenderId: "ВАШ_MESSAGING_SENDER_ID",  // ← Вставьте сюда
    appId: "ВАШ_APP_ID"  // ← Вставьте сюда
};
```

**Или** используйте переменные окружения (рекомендуется для продакшена).

### 8.3 Проверка подключения Firebase SDK

Убедитесь, что во всех HTML файлах подключены Firebase SDK:

```html
<!-- Firebase SDK -->
<script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-app-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-auth-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-storage-compat.js"></script>

<!-- Firebase Config -->
<script src="js/firebase-config.js"></script>
```

**✅ Шаг 8 завершен!** Код обновлен.

---

## 🧪 Шаг 9: Тестирование подключения

### 9.1 Локальное тестирование

1. Откройте `index.html` в браузере
2. Откройте консоль разработчика (F12)
3. Проверьте, что нет ошибок Firebase
4. Попробуйте выполнить в консоли:

```javascript
// Проверка инициализации
console.log(isFirebaseInitialized()); // должно быть true

// Проверка Firestore
console.log(getFirestore()); // должен вернуть объект

// Проверка Auth
console.log(getAuth()); // должен вернуть объект
```

### 9.2 Переключение на Firebase режим

В консоли браузера выполните:

```javascript
setAPIMode('firebase');
```

### 9.3 Проверка работы

1. Попробуйте добавить товар в корзину
2. Проверьте, что данные сохраняются в Firestore
3. Откройте Firebase Console > Firestore Database
4. Убедитесь, что данные появились

**✅ Шаг 9 завершен!** Подключение работает.

---

## 🌐 Шаг 10: Настройка домена для продакшена

### 10.1 Получение домена Vercel

После деплоя на Vercel вы получите домен вида: `dostavka-xxx.vercel.app`

### 10.2 Добавление домена в Firebase

1. В Firebase Console перейдите в **Authentication**
2. Откройте вкладку **Settings** (Настройки)
3. Прокрутите до раздела **Authorized domains** (Авторизованные домены)
4. Нажмите **Add domain** (Добавить домен)
5. Введите ваш домен Vercel (например: `dostavka-xxx.vercel.app`)
6. Нажмите **Add** (Добавить)

**✅ Шаг 10 завершен!** Домен настроен.

---

## ✅ Чеклист завершения

Проверьте, что все шаги выполнены:

- [ ] Проект Firebase создан
- [ ] Firestore Database создана и настроена
- [ ] Authentication включена (Email/Password)
- [ ] Storage создан и настроен
- [ ] Правила безопасности Firestore установлены
- [ ] Правила безопасности Storage установлены
- [ ] Конфигурация Firebase получена
- [ ] Код проекта обновлен с конфигурацией
- [ ] Локальное тестирование пройдено
- [ ] Домен добавлен в Authorized domains

---

## 🎉 Готово!

Firebase полностью интегрирован в ваш проект!

### Что дальше?

1. **Деплой на Vercel** - см. инструкцию `SETUP_FULL_SERVICE.md`
2. **Добавление переменных окружения** в Vercel
3. **Тестирование** на продакшене

### Полезные ссылки

- [Firebase Console](https://console.firebase.google.com/)
- [Firebase Documentation](https://firebase.google.com/docs)
- [Firestore Documentation](https://firebase.google.com/docs/firestore)

---

## 🐛 Решение проблем

### Проблема: "Firebase не инициализирован"

**Решение:**
1. Проверьте, что Firebase SDK загружен
2. Проверьте конфигурацию в `firebase-config.js`
3. Проверьте консоль браузера на ошибки

### Проблема: "Permission denied"

**Решение:**
1. Проверьте правила безопасности Firestore/Storage
2. Убедитесь, что пользователь авторизован
3. Проверьте роль пользователя в базе данных

### Проблема: "CORS error"

**Решение:**
1. Добавьте домен в Authorized domains в Firebase
2. Проверьте настройки CORS в Firebase Console

