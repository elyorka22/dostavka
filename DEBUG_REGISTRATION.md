# 🔍 Диагностика проблемы с регистрацией

## Как проверить, что не работает

### Шаг 1: Откройте консоль браузера

1. Откройте сайт на Vercel
2. Нажмите **F12** (или правый клик → Inspect → Console)
3. Перейдите на страницу входа

### Шаг 2: Проверьте инициализацию Firebase

В консоли должны появиться сообщения:
- `"Переключение на Firebase режим..."` (в продакшене)
- `"API_MODE установлен в: firebase"` (в продакшене)
- `"Локальный режим..."` (локально)

### Шаг 3: Попробуйте зарегистрироваться

1. Нажмите вкладку **"Ro'yxatdan o'tish"**
2. Заполните форму:
   - Имя: `Test User`
   - Email: `test@example.com`
   - Пароль: `test123456`
   - Подтверждение пароля: `test123456`
3. Нажмите **"Ro'yxatdan o'tish"**

### Шаг 4: Проверьте сообщения в консоли

Должны появиться сообщения:
- `"Регистрация через Firebase, проверка инициализации..."`
- `"Создание пользователя в Firebase Auth..."`

Если есть ошибки, они будут показаны в консоли.

## 🔧 Возможные проблемы и решения

### Проблема 1: "Firebase SDK не загружен"

**Решение:**
- Проверьте, что все скрипты Firebase загружены
- Проверьте интернет-соединение
- Обновите страницу (Ctrl+F5)

### Проблема 2: "Firebase Auth не инициализирован"

**Решение:**
- Проверьте, что Firebase правильно настроен
- Проверьте переменные окружения в Vercel
- Убедитесь, что `initFirebase()` вызывается

### Проблема 3: "Bu email allaqachon ishlatilgan"

**Решение:**
- Этот email уже используется
- Используйте другой email
- Или войдите с этим email

### Проблема 4: "Internet aloqasi yo'q"

**Решение:**
- Проверьте интернет-соединение
- Проверьте, что Firebase доступен
- Попробуйте позже

### Проблема 5: "register funksiyasi topilmadi"

**Решение:**
- Обновите страницу (Ctrl+F5)
- Проверьте, что `js/users.js` загружен
- Проверьте консоль на ошибки загрузки скриптов

## 📋 Чек-лист проверки

- [ ] Firebase SDK загружен (проверьте Network в DevTools)
- [ ] Firebase инициализирован (сообщение в консоли)
- [ ] API_MODE установлен в 'firebase' (в продакшене)
- [ ] Функция register доступна (проверьте в консоли: `typeof register`)
- [ ] Нет ошибок в консоли при загрузке страницы
- [ ] Форма регистрации отображается
- [ ] Кнопка "Ro'yxatdan o'tish" работает

## 🧪 Тест в консоли браузера

Выполните в консоли браузера:

```javascript
// Проверить Firebase
console.log('Firebase:', typeof firebase !== 'undefined' ? 'загружен' : 'не загружен');
console.log('initFirebase:', typeof initFirebase !== 'undefined' ? 'определена' : 'не определена');
console.log('isFirebaseInitialized:', typeof isFirebaseInitialized !== 'undefined' ? isFirebaseInitialized() : 'не определена');

// Проверить API_MODE
console.log('API_MODE:', typeof API_MODE !== 'undefined' ? API_MODE : 'не определен');

// Проверить функцию register
console.log('register:', typeof register !== 'undefined' ? 'определена' : 'не определена');
console.log('window.register:', typeof window.register !== 'undefined' ? 'определена' : 'не определена');

// Попробовать инициализировать Firebase
if (typeof initFirebase !== 'undefined') {
    var initialized = initFirebase();
    console.log('Firebase инициализирован:', initialized);
}
```

## 📞 Если ничего не помогает

1. Скопируйте все сообщения из консоли
2. Скопируйте ошибки (красные сообщения)
3. Проверьте Network tab - есть ли ошибки загрузки скриптов
4. Сообщите о проблеме с этими данными

## ✅ Что должно работать

После исправлений:
- ✅ Детальное логирование в консоли
- ✅ Понятные сообщения об ошибках на узбекском
- ✅ Автоматическая инициализация Firebase
- ✅ Правильная обработка всех ошибок
- ✅ Регистрация работает в Firebase и localStorage

