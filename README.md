# Mahsulot yetkazib berish xizmati

Сервис доставки продуктов для города Ташкент.

## 🚀 Быстрый старт

### Локальная разработка

```bash
# Запустить локальный сервер
python3 -m http.server 8000

# Или через npm
npm start
```

Откройте браузер: http://localhost:8000

## 📦 Деплой

### Vercel

1. Подключите репозиторий к Vercel
2. Vercel автоматически определит настройки из `vercel.json`
3. Добавьте переменные окружения (см. `env.example`)
4. Деплой произойдет автоматически

### Railway

1. Подключите репозиторий к Railway
2. Railway автоматически определит настройки из `railway.json`
3. Добавьте переменные окружения
4. Деплой произойдет автоматически

## 🔧 Настройка

### Переменные окружения

Скопируйте `env.example` в `.env` и заполните значения:

```bash
cp env.example .env
```

### Firebase

1. Создайте проект в [Firebase Console](https://console.firebase.google.com/)
2. Настройте Authentication, Firestore, Storage
3. Скопируйте конфигурацию в `js/firebase-config.js` или используйте переменные окружения

## 📚 Документация

- [Инструкция по деплою](DEPLOYMENT.md)
- [Статус проекта](PROJECT_STATUS.md)
- [Чеклист перед деплоем](PRE_DEPLOYMENT_CHECKLIST.md)

## 🛠 Технологии

- HTML5
- CSS3
- Vanilla JavaScript
- Firebase (Firestore, Authentication, Storage)
- Vercel / Railway для хостинга

## 📝 Лицензия

MIT

